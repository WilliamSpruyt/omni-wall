{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd client && npm install && npm run build && cd .. && node server.js",
    "heroku-postbuild": "cd client && npm install && npm run build && cd .. && node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.4",
    "dotenv": "^6.0.0",
    "express": "^4.16.3",
    "mobile-device-detect": "^0.2.3",
    "mongoose": "^5.3.2",
    "morgan": "^1.9.1",
    "react-device-detect": "^1.6.1",
    "react-share": "^2.3.1"
  },
  "engines": {
    "node": "10.9.0",
    "npm": "6.1.0"
  }
}
